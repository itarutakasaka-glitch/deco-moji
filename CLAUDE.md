# CLAUDE.md — decomoji.xyz（デコ文字メーカー）

Itaru（itarutakasaka-glitch / itaru.takasaka@gmail.com）の個人サイト。会話は日本語。

## このリポは何か
- **decomoji.xyz** = デコ文字メーカー。特殊文字（デコ文字）の変換ツール＋関連コンテンツ。
- Stack: Next.js（App Router）/ Vercel 自動デプロイ（main push → 本番）/ メールは Resend 予定。
- GitHub: `itarutakasaka-glitch/deco-moji`（PRIVATE）。本番ホストは **www.decomoji.xyz**（apex はリダイレクト）。
- 計測: GA4 = `G-GDX0LHQ0BG`（GAアカウント mall-japan 配下）。AdSense pub = `pub-7490892593665830`。

## 最重要の現状（2026-07-11 時点）
- **AdSense 再審査を 2026-07-11 にリクエスト済み（ステータス「準備中／審査待ち」を実機確認）**。不承認理由は「有用性の低いコンテンツ」だった。広告タグは配信中だが実広告はまだ。
  - AdSense管理画面に「サイトの同意メッセージ（CMP）を作成する」の未設定項目あり（EEA向け・審査自体には必須でない）。承認後に設定を検討。
- GSC: itaru.takasaka@gmail.com 配下・URLプレフィックスで設定済み。`/`・`/play`・`/trash-day` インデックス済み（6/30時点）。検索表示は「デコ文字メーカー」ブランド語＋「目黒区 ゴミ カレンダー」系クエリで発生中（クリック16・表示112・平均順位8.1）。
- 再審査コースの進捗:
  - ✅ ドアウェイ3ページ（/instagram /tiktok /x）を独自ガイドに転生（不承認の直接原因を除去）
  - ✅ /shindan（SNSパワー診断・動的OGP）/ /kabegami（推しメンカラ壁紙・13グループ）
  - ✅ /copy（コピペ集10カテゴリ）/ /mojibake（文字化け早見表）
  - ✅ サイトURL修正（canonical等が decomoji.example.com プレースホルダを指していた重大バグを修正）
  - ✅ **ごみ収集日カレンダー＋ゴミ出し占い `/trash-day`（動的OGP）** をローンチ／`/play` 診断・占いポータル／`/compatibility` デコ文字相性診断も公開
  - ✅ /trash-day を目黒区公式サイト風デザイン（紫×白）＋インフォグラフィック化（Gemini生成のヒーロー/種別アイコン＝public/gomi-hero.png・icon-*.png）
  - ✅ **マルチ自治体化・東京21区 約2,460丁目に拡大**（中野・千代田を除く全21区）。区セレクタ＋区ごとの分別区分。内訳：7区=東京都オープンデータ（CC BY）／8区=区公式HTML表/CSVスクレイプ／**6区（豊島・足立・葛飾・北・墨田・港）=公式PDF/カレンダーを画像描画し「vision（画像目視）＋実データ検算」でデータ化**（tesseract OCRは精度不足で不採用。pymupdf描画→サブエージェント精読→本体で独立再検証。港は64枚のグループカレンダーを4バッチ並行精読、赤坂1丁目=Group1が全バッチ一致で整合検証）。15区は本番反映済み（PR #1〜#4）／豊島他4区はPR #5／墨田・港はPR #6。
  - **残り2区は取り込み不可（実質これが上限）**：中野=収集日データ自体が無い／千代田=公式早見表PDFが2019年版で古く番地/奇偶が複雑。
  - ✅ **AdSense「審査をリクエスト」実行（2026-07-11）→ 結果待ち**
  - ⏳ 残: 審査結果待ち／ゴミ収集日メール通知（下記）／診断の拡充・ブログ増強

## 進行中の新機能：ゴミ出し占い × ごみ収集日メール通知
- コンセプト: 「占いが"今日の正しいゴミの出し方（開運作法）"を授ける」。義務を娯楽化。**主役はごみ収集日チェッカー、占いは調べた結果の下に出る二次導線**。
- 実装済み（Web版）: `/trash-day` = ごみ収集日カレンダー（区セレクタ＋丁目セレクタ）→ 結果の下の導線から占い。占いカード＋結果ページ `r/[slug]`（noindex・slug=`yyyymmdd-丁目index`）＋動的OGP `og/[slug]`。コアは `lib/gomi/core.ts`（TZ非依存・丁目パラメータ化）、UIは `components/GomiFortune.tsx` / `FortuneCard.tsx`。旧 `/trash-fortune` は308リダイレクト。
- 占いエンジン = **軸の掛け算**（/shindan のシード乱数を流用）。ごみ種別＝テーマ × 断捨離 × 風水 × ネタ。シードに日付＋丁目を含め決定論的。年末年始（12/29〜1/3）は警告バナー＋区公式リンク（特別日程の実日付は未データ化）。
- 配信（未着手）= **メール**（LINE Notify は2025-03終了のため）。Resend + Vercel Cron（毎朝6時JST）。デコ文字装飾＋アフィリ＋サイト誘導。
- 法律: 特定電子メール法（opt-in＋解除リンク＋送信者情報）、ステマ規制（PR明記）。AdSenseはメール内不可→収益はアフィリ＋サイト誘導でAdSense。
- データ: `lib/gomi/<区>-schedule.json`（目黒/品川/台東/江東/文京/中央/大田）。正本CSV/XLSXは `source/`。区ごとに分別区分・種別呼称・出典が異なる（`MUNICIPALITIES` 配列＋`singleChomeMuni()`で正規化）。分別区分は5固定＋**plastic**（プラスチック、`public/icon-plastic.png`）。区で正確に扱えない区分（江東=隔週の不燃、中央=粗大）は `omitNote` で明示し**載せない**（誤情報回避）。
- ルール形式は汎用（weekly / nthWeekday）。**新しい区は必ず MUNICIPALITIES の末尾に追加**（slug後方互換＝既存の丁目indexを動かさない）。
- **東京23区の開放データ現状（2026-07-12調査）**: 収集**日**の機械可読オープンデータがあるのは7区（目黒/品川/台東/江東/文京/中央/大田）のみ。他区（中野など）は集積所位置・分別品目しか公開しておらず収集日データが無い。隔週収集の区は基準週データが無く算出不能。
- **区公式サイトのスクレイプ（2026-07-12〜）**: 開放データが無い区でも公式サイトに町丁目別の収集日**表**があればスクレイプで取り込み可能。世田谷区（`02241/416.html` のHTML表・pandas.read_html）を第1弾として追加（277丁目）。ペットボトル区分は plastic 枠（PETボトルアイコン）に割当。曜日の省略表記「水・土曜日」「2・4回目」に対応するパーサ改良済。※開放データでないので出典文言は「公式収集日情報」に一般化。
- 次の一手候補: ①年末年始の特別日程をデータ化 ②メール通知（Resend+Cron）＋法務対応 ③`/play` ポータルに診断を増やす ④残り区は区サイト/PDFスクレイプ（要・重い裏取り、非推奨）。

## 開発スタンス（Itaru）
- 非エンジニア。MVP-First（単体HTMLで世界観→確認→Next.js統合）。選択肢A/B/Cで決断を求める。短く結論先出し。
- ミスは隠さず率直に。間違ったまま完了主張しない。
- **ファン向けデータ（メンカラ/曜日等）は必ずWebで裏取り**（記憶のメンカラは複数回間違えた実績あり）。アーティスト写真・ロゴは権利上不可、名前×事実情報のみ。
- 各変更後にスクショ/プレビューで視覚確認するイテレーティブな進め方。

## 運用ルール（恒久・グローバルCLAUDE.md準拠）
- **作業の区切りで commit → push 必須**（Git管理＋GitHubバックアップ）。push は許可済み（`~/.claude/settings.json` の permissions で `Bash(git push:*)` 自動許可。force系のみ確認）。
- ディレクトリ/URL/ファイル/API名は英語（日本語ローマ字禁止）。コメント/UI文言は日本語OK。
- dev サーバ稼働中に本番ビルドすると `.next` が壊れる→ビルド時はdev停止。Webhook取りこぼしでデプロイされない時はVercelで手動Redeploy。

## 兄弟プロジェクト
- **bluetanuki.xyz**（`../hatch-site`）: 5ジャンル銀河の便利ツール集。GA4=`G-Y7T0SJLPYG`稼働中・AdSense未申請。引き継ぎは repo 内 HANDOVER.md。
- 個人系は全て `C:\Users\kyari\projects\other\` 配下（deco-moji / hatch-site / deco-moji-mvp）。MVPの単体HTMLは `../deco-moji-mvp`。
- ※Toggle/ヘヤクレスの仕事は `C:\Users\kyari\projects\` 直下の別プロジェクト。混ぜない。
